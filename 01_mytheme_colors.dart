// ============================================================
//  SKIN "MEDIANOCHE" — reemplaza el bloque de colores de MyTheme
//  Archivo: flutter/lib/common.dart  (clase MyTheme, ~línea 253)
// ============================================================

  // --- Paleta Medianoche ---
  static const Color grayBg = Color(0xFFF4F6FA);        // fondo claro (antes EFEFF2)
  static const Color accent = Color(0xFF378ADD);        // acento cian (antes 0071FF)
  static const Color accent50 = Color(0x77378ADD);
  static const Color accent80 = Color(0xAA378ADD);
  static const Color canvasColor = Color(0xFF1A1D29);   // lienzo oscuro Medianoche
  static const Color border = Color(0xFFD7DCE5);
  static const Color idColor = Color(0xFF5DCAA5);        // ID en verde menta de acento
  static const Color darkGray = Color.fromARGB(255, 148, 148, 148);
  static const Color cmIdColor = Color(0xFF0F6E56);
  static const Color dark = Colors.black87;
  static const Color button = Color(0xFF378ADD);         // botones (antes 2C8CFF)
  static const Color hoverBorder = Color(0xFF8FA3BF);
